export default async function handler(req,res){res.setHeader("Access-Control-Allow-Methods","POST,OPTIONS");if(req.method==="OPTIONS")return res.status(200).end();if(req.method!=="POST")return res.status(405).json({error:"Method not allowed"});const {name="",dob="",time="",location="",topic="Personal growth"}=req.body||{};if(!name.trim()||!dob||!time||!location.trim())return res.status(400).json({error:"Name, date, time and birthplace are required"});const seed=(new Date(dob).getDate()+Number(time.slice(0,2)||0)+name.trim().length+location.trim().length+topic.length)%5;const a=["Your chart theme points toward progress through consistency. Choose one meaningful direction and build it patiently.","You may gain clarity by balancing intuition with practical action. The next small step matters more than a perfect plan.","This period favors focus. Reduce distractions and pay attention to opportunities that keep returning.","Your energy is strongest when you create structure around what matters most instead of reacting to every new possibility.","Reflection can reveal patterns, but movement creates momentum. Turn one insight into a simple action today."];res.status(200).json({reading:name.trim()+", "+a[seed],birthSummary:"Reading based on the birth details you entered: "+dob+" at "+time+", "+location.trim()+".",disclaimer:"For entertainment and personal reflection only. This is not a scientific birth-chart calculation."})}